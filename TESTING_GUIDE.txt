================================================================================
TESTING GUIDE - Featured Properties City Search & Browse Tab Fixes
================================================================================

BUGS FIXED:
1. Cities like "Kurunegala" now filter correctly in Featured Properties
2. Featured Properties filtering no longer affects the Browse tab

================================================================================
TEST SCENARIO 1: City Search in Featured Properties
================================================================================

Steps:
1. Go to Home page (Featured Properties section)
2. Locate the "Search by City" input field above "View all listings" button
3. Type "Kurunegala" in the search box

Expected Result:
✓ Properties in Kurunegala should appear
✓ Properties from other cities should disappear
✓ Shows only matching properties

Test with different cities:
- Try: Colombo, Kandy, Galle, Rathnapura, Ruwanwella
- Result: Only properties from that city should show

Test with partial names:
- Try: "Kuru" (should find Kurunegala)
- Try: "Rata" (should find Rathnapura)
- Try: "Col" (should find Colombo)
- Result: Partial matches should work

Clear the search:
- Delete all text from search box
- Result: All properties should reappear (based on type filter)

================================================================================
TEST SCENARIO 2: Type Filter in Featured Properties
================================================================================

Steps:
1. Go to Home page
2. Click filter buttons: All, For Sale, For Rent, Apartments, Houses, Villas

Expected Result:
✓ Each filter shows correct property types
✓ When you click a filter, that type's properties appear
✓ Works independently from city search

Test combination:
1. Set Type Filter to "House"
2. Type "Colombo" in city search
3. Result: Should show only HOUSES in COLOMBO

Another combination:
1. Set Type Filter to "Villa"
2. Type "Kurunegala" in city search
3. Result: Should show only VILLAS in KURUNEGALA

================================================================================
TEST SCENARIO 3: Browse Tab Independence
================================================================================

Steps:
1. Go to Home page - Featured Properties section
2. Type city in search box (e.g., "Colombo")
3. Click filter to type (e.g., "House")
4. Go to Browse tab (click "Browse" in navbar OR "View all listings" button)

Expected Result:
✓ Browse tab should show ALL UNFILTERED PROPERTIES
✓ Featured Properties city search does NOT affect Browse tab
✓ Featured Properties type filter does NOT affect Browse tab
✓ Browse tab has its own independent filters (sidebar)

Verify independence:
1. In Featured Properties: Filter by "Colombo" and "House"
   → Featured Properties shows filtered results
2. Click "View all listings" OR navigate to Browse tab
   → Browse tab shows all properties
   → Featured Properties filters did NOT carry over
3. In Browse tab: Apply filters (Price, Bedrooms, City, Type)
   → Browse tab filters work independently
   → Featured Properties remains unaffected

================================================================================
TEST SCENARIO 4: Case Insensitive Search
================================================================================

Steps:
1. Type in Featured Properties city search

Test cases:
- "kurunegala" (lowercase) → should find Kurunegala
- "KURUNEGALA" (uppercase) → should find Kurunegala
- "Kurunegala" (proper case) → should find Kurunegala
- "KuRuNeGaLa" (mixed case) → should find Kurunegala

Expected Result:
✓ All variations should find the same results
✓ Case should not matter

================================================================================
TEST SCENARIO 5: No Results
================================================================================

Steps:
1. Go to Featured Properties
2. Type a city name that has no properties (e.g., "Xyz" or "Nonexistent")

Expected Result:
✓ Message shows: "No properties match your search."
✓ Grid should be empty
✓ Clearing the search shows properties again

================================================================================
TEST SCENARIO 6: Page Load and Navigation
================================================================================

Steps:
1. Refresh the page
2. Featured Properties should show all properties by default
3. Search should be empty
4. Type filter should be set to "All"

Expected Result:
✓ All properties load on page load
✓ SearchBy City input is empty
✓ "All" filter button is active
✓ Page is clean and ready to search

Navigate away and back:
1. Features Properties → Type filter "House"
2. Go to Browse tab
3. Come back to Home tab
4. Type filter should be reset to "All"

Expected Result:
✓ Filter state resets when you come back to home
✓ Each page maintains its own state

================================================================================
TEST SCENARIO 7: Real Property Data
================================================================================

Current properties in system (example cities):
- Colombo (multiple properties)
- Kurunegala
- Rathnapura
- Kandy
- Galle

Test with actual data:
1. Search "Colombo" → See Colombo properties
2. Search "Kurunegala" → See Kurunegala property
3. Search "Rathnapura" → See Rathnapura property
4. Combine: Filter "House" + Search "Colombo"
   → See only Colombo houses

================================================================================
DEBUGGING CHECKLIST
================================================================================

If city search is NOT working:

□ Check browser console for JavaScript errors
  - Open DevTools (F12)
  - Check Console tab for red errors

□ Verify window.properties is populated
  - In browser console, type: window.properties
  - Should see array of property objects
  - Each should have: id, title, price, location, type, status, etc.

□ Check if searchHomeByCity function exists
  - In browser console, type: typeof searchHomeByCity
  - Should return: "function"

□ Verify property location format
  - In console: window.properties[0].location
  - Should return city name (e.g., "Colombo", "Kurunegala")

If Browse tab is affected by Featured search:

□ Verify filterHome function doesn't modify window.properties
□ Check that applyFilters() uses independent filter state
□ Browse tab should use currentFilters variable, not homeFilterType/homeSearchCity
□ Featured Properties should use homeFilterType/homeSearchCity, not currentFilters

================================================================================
PERFORMANCE NOTES
================================================================================

- Re-rendering grid on each keystroke is fast (small property list)
- If large dataset added later, consider debouncing search
- Current implementation rebuilds HTML of grid each filter change

================================================================================
CODE LOCATIONS
================================================================================

Featured Properties Filtering:
- File: src/main/webapp/app.js
- Functions:
  - initHome() - Initialize home page
  - filterHome(btn, filterType) - Handle type filter clicks
  - searchHomeByCity(searchTerm) - Handle city search
  - renderHomeFeaturedProperties() - Re-render grid after filters change

Search Input UI:
- File: src/main/webapp/index.jsp
- Line: ~1199
- ID: home-city-search
- Event listener: DOMContentLoaded → calls searchHomeByCity()

Browse Tab Filtering:
- File: src/main/webapp/app.js
- Functions:
  - applyFilters() - Apply browse tab filters
  - renderListings() - Re-render browse grid
- Uses: currentFilters state variable (independent from Featured)

================================================================================
VERIFICATION CHECKLIST
================================================================================

Before deploying, verify:

□ No JavaScript errors in browser console
□ Featured Properties search works
□ Featured Properties type filters work
□ Browse tab filters work independently
□ Browse tab is not affected by Featured Properties filters
□ Case-insensitive search works
□ Partial city name search works
□ Both filters work together (city + type)
□ Page refreshes load all properties by default
□ No console errors on page load
□ Search input has proper styling and placeholder text
□ "Search by City" label is visible above input

================================================================================

